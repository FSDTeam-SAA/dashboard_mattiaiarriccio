# Full backend API base URL (the dashboard calls the backend directly — no proxy).
# Set this to your VPS backend URL, e.g. http://YOUR_SERVER_IP:PORT/api/v1 or https://api.yourdomain.com/api/v1
NEXT_PUBLIC_API_BASE_URL=http://187.77.187.56:8082/api/v1
